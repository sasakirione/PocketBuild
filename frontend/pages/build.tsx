import PokemonList from "../components/elements/build/PokemonList";

const build = (): JSX.Element => {
    return (
        <div>
            <h1>編成</h1>
            <PokemonList/>
        </div>
    )
}

export default build